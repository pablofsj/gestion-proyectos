# Copia este archivo a `.env` y ajusta los valores si es necesario.

# Conexión a PostgreSQL (credenciales definidas en el enunciado)
DATABASE_URL="postgresql://root:desarrollo_software_1@localhost:5432/desarrollo_software_1"

# Clave para firmar/verificar los JWT. Genera una nueva con:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="genera-un-secreto-de-32-bytes"
